class ClassObject {
    x = 10;

    method start() {
        // Creating an instance of the ClassObject class
        co = new ClassObject();
        
        // Accessing the 'x' member variable of the object 'co'
        print(co.x) // Outputs: 10
    }
}
