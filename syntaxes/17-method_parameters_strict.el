class MethodParameters {

    method start() {
        tage = age()
        print(tage)
    }

    method int age() {
        return 10 # this function will only return int value, if you try to return
        // any other type of value, then it will throw error
    }

    // i am not adding strict parameter here,
    // if it will necessary, then it will be added in future

}
