class ArrayIteration {
    method start() {
        
        // array with default values
        arr = [12, 15.2, 's', 18.7, "test", 'EasyLang']

        # Iterate over the array
        whileeach (element in arr) {
            print(element) # will Iterate and print each element in the array
        }
    }
}
